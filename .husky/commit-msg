# # Please remove the following two lines from .husky/commit-msg:
# # They WILL FAIL in v10.0.0
# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

echo "🔍 Running commit message lint/check ..."

npx --no -- commitlint --edit "$1"
